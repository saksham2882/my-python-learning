import PY_29_saksham

PY_29_saksham.welcome()    
# We printed it only once, but the output shows it twice.  
# This means if we just write 'import CWH_45_saksham', it will print once.  
# To avoid this problem, we use 'if __name__ == "__main__":' in the function file.


# NOTE: To see the imported function, check the file -> PY_29_saksham
